# ผลตรวจการแก้ไข — 7 กันยายน 2026

- `npm run check` ผ่าน: syntax JavaScript หน้าเว็บและ API
- `npm test` ผ่าน 28 กรณี: PostgreSQL ผ่าน PGlite, migration ข้อมูลเดิม, API ที่ mock network, DOM และ local server
- `npm run test:concurrency` ผ่าน 5 กรณี: PostgreSQL 18 ชั่วคราวหลาย connection บน localhost มีการยืนยันว่า transaction ที่สองรอ database lock จริง
- `npm run build` ผ่าน: สร้าง public assets 6 ไฟล์
- `npm audit` ไม่พบช่องโหว่ใน dependency ที่ติดตั้ง ณ เวลาตรวจ ไม่ครอบคลุมสคริปต์จาก CDN หรือการตั้งค่าบริการภายนอก

กรณีสำคัญที่ผ่าน: รหัสผ่าน null/ว่าง, ล็อกบัญชีเมื่อผิด 5 ครั้ง, ปิดสิทธิ์ตารางสาธารณะ, ตรวจ role ฝั่งฐานข้อมูล, คำขอออกบัตรซ้ำ, rollback ทั้งชุดเมื่อโควตาไม่พอ, QR แบบสุ่ม, ลิงก์ลูกค้าต้องมี token, สแกนซ้ำ, ยกเลิกพร้อมสแกนทั้งสองลำดับ, แย่ง VIP ใบสุดท้าย, รายงานเกินหนึ่งหน้า, บันทึกชำระ/คืนเงิน, ยกเลิก session, ส่ง LINE ด้วยข้อมูลจากฐานข้อมูล, retry เดิมเมื่อผลไม่แน่นอน และการกู้คำขอหลังเน็ตหลุด

ยังไม่ได้ดำเนินการกับระบบจริง: รัน migration บน Supabase ของผู้ใช้, deploy Vercel, ส่ง LINE จริง, ทดสอบกล้องมือถือจริง และสำรอง/restore ฐานข้อมูลของผู้ใช้ เนื่องจากงานนี้ทดสอบในเครื่องและไม่มีข้อมูลเชื่อมต่อฐานข้อมูลสำหรับผู้ดูแลหรือปลายทาง deploy ที่ยืนยันแล้ว

เตรียม `scripts/backup.ps1`, `bootstrap-admin.example.sql` และขั้นตอนตรวจรับ/กู้คืนไว้ใน `UPGRADE.md` แล้ว ไม่มีการสร้างรหัสผ่านจริงไว้ใน source
