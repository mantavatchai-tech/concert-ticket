{
  "name": "concert-ticket-checkin",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node scripts/dev-server.cjs",
    "check": "node --check app.js && node --check ticket.js && node --check api/send-line-ticket.js && node --check api/qr.js && node --check api/line-webhook.js",
    "test": "node --test tests/*.test.cjs",
    "build": "node scripts/build.cjs",
    "test:concurrency": "node --test tests/concurrency.cjs",
    "demo": "node scripts/local-demo.cjs"
  },
  "dependencies": {
    "fflate": "^0.8.3",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8",
    "embedded-postgres": "^18.4.0-beta.17",
    "jsdom": "^30.0.1",
    "pg": "^8.23.0"
  },
  "engines": {
    "node": ">=22"
  }
}
