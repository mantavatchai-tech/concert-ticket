# คู่มือใช้งาน

## เริ่มงาน

แอดมินเข้าสู่ระบบแล้วเพิ่มวันงาน ชื่อ ราคา VIP รายการราคา Regular และจำนวนสูงสุดใน “เพิ่ม / แก้ไขงาน” เลือกวันเดิมเพื่อโหลดค่ามาแก้ไข วันงานที่ผ่านไปแล้วเก็บไว้ดูรายงานและไม่สามารถออกบัตรใหม่

สร้างเจ้าหน้าที่ใน “จัดการบัญชี” โดยใช้รหัสผ่านเฉพาะบุคคล 12–72 ไบต์ บัญชีใหม่ต้องระบุรหัสผ่าน บัญชีเดิมเว้นว่างเพื่อคงรหัสผ่านเดิม การแก้บัญชีหรือปิดใช้งานจะยกเลิก session ของบัญชีนั้น

| สิทธิ์ | การใช้งาน |
|---|---|
| admin | จัดการงาน บัญชี ออกบัตร ชำระเงิน เช็คอิน รายงาน และ audit |
| issuer | ออกบัตร แก้ราคา ยกเลิก บันทึกชำระ/คืนเงิน ส่ง LINE และ Export |
| checkin | สแกน/กรอกรหัส ดูยอดและประวัติเช็คอิน ไม่มีรายชื่อผู้ซื้อหรือรายการ QR ทั้งหมด |

## ออกบัตร

เลือกวัน ประเภท ราคา จำนวน ชื่อลูกค้า และ LINE userId ถ้าไม่ต้องการส่ง LINE ให้เอาเครื่องหมายส่ง LINE ออก VIP หนึ่งใบมี 4 QR ส่วน Regular หนึ่งใบมี 1 QR

กดสร้างครั้งเดียว ระบบออกทั้งชุดใน transaction เดียว ถ้าอินเทอร์เน็ตขาดและยังไม่ทราบผล ให้ใช้ “กู้คืนรายการค้าง” จาก browser และบัญชีเดิม ปุ่มนี้จะใช้ request ID เดิม ไม่สร้างบัตรชุดที่สอง อย่าล้างข้อมูล browser ระหว่างมีรายการค้าง

รายการค้างเก็บชื่อและ LINE userId ชั่วคราวใน localStorage และลบเมื่อทราบผลสำเร็จ อุปกรณ์เจ้าหน้าที่ควรมีบัญชีผู้ใช้งานแยกกัน

## ชำระเงินและรายงาน

บัตรใหม่เริ่มที่รอชำระ ข้อมูลเก่าเริ่มที่ยังไม่ตรวจสอบ ใช้ “บันทึกชำระ / คืนเงิน” หลังตรวจเงินแล้ว ยอดชำระต้องตรงราคาบัตรและมีเลขอ้างอิง ข้อมูลผู้ยืนยันและประวัติการแก้ไขถูกบันทึกอัตโนมัติ

บัตรที่ชำระแล้วแก้ราคาไม่ได้ หากบันทึกคืนเงินต้องระบุเลขอ้างอิงและยอดรับสุทธิเป็น 0 การยกเลิกบัตรไม่คืนเงินจริงอัตโนมัติ จึงยังนับเงินที่รับไว้จนเจ้าหน้าที่บันทึกคืนเงิน

Dashboard “รับเงินจริงสุทธิ” นับจากสถานะชำระแล้ว ส่วน Export แยกยอดรับจริงจากมูลค่าบัตร รายงานอ่านครบทุกหน้า ขณะ Export ควรหลีกเลี่ยงแก้การเงินพร้อมกันหากต้องการยอดกระทบยอด ณ เวลาเดียว

## ส่ง LINE และพิมพ์

API อ่านผู้รับที่ผูกกับบัตรจากฐานข้อมูล ไม่มีช่อง PIN ร่วม ส่งข้อความลิงก์บัตรให้ลูกค้าเปิด QR ลิงก์เป็นกุญแจเข้าถึงบัตร ห้ามส่งต่อให้บุคคลอื่น

หากส่งล้มเหลว ใช้ปุ่มส่งอีกครั้งที่บัตรเดิม ระบบใช้ retry key เดิมตามแนวทาง [LINE Messaging API](https://developers.line.biz/en/docs/messaging-api/retrying-api-request/) เมื่อ LINE รับคำขอแล้วจะไม่ส่งซ้ำอีก หากผลยังไม่แน่นอนเกิน 23 ชั่วโมง ระบบหยุดส่งอัตโนมัติ ต้องตรวจประวัติ LINE ก่อนดำเนินการต่อ ไม่รีเซ็ต retry key โดยไม่ตรวจผล

Webhook บันทึกลูกค้าจาก LINE OA ส่งข้อความต้อนรับเมื่อบันทึกลูกค้าใหม่สำเร็จ (best-effort); ใช้ greeting ของ LINE OA เสริมได้ เมื่อมีข้อผิดพลาดในการบันทึกฐานข้อมูล ระบบส่งสถานะล้มเหลวเพื่อให้ LINE ส่ง webhook ซ้ำ

ปุ่มพิมพ์เปิดหน้าลูกค้าและเรียกหน้าพิมพ์ เบราว์เซอร์อาจให้ยืนยันการพิมพ์อีกครั้ง

## เช็คอิน

เลือกวันงานและกรอกชื่อเจ้าหน้าที่ก่อนสแกน กล้องต้องใช้ HTTPS และ browser ที่รองรับ BarcodeDetector หากเปิดกล้องไม่ได้ใช้กรอกรหัสจากบัตรเป็นทางสำรอง

ระบบใช้วันที่ไทยจากฐานข้อมูลตรวจวันงานและล็อกแถวบัตรเพื่อกันการสแกนซ้ำ/ยกเลิกพร้อมกัน ไม่อนุญาตเช็คอินแบบ offline เพราะไม่สามารถยืนยันการใช้ซ้ำจากหลายเครื่องได้

## อัปเดต / สำรอง / กู้คืน

ทำตาม [UPGRADE.md](UPGRADE.md) และตั้งค่าระบบตาม [README.md](README.md)
