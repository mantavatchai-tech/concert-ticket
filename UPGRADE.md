# การอัปเดตและกู้คืน

## ก่อนอัปเดตจริง

ทำช่วงหยุดขายและหยุดเช็คอิน เพราะ SQL และหน้าเว็บต้องใช้เวอร์ชันเดียวกัน สำรองฐานข้อมูลก่อนและเก็บ source รุ่นเดิมแยกไว้

`security-upgrade.sql` จะ:

- ปิดการอ่านข้อมูลสาธารณะและเปลี่ยน RPC
- ยกเลิก session เดิมทุกบัญชี
- ปิดบัญชีที่ยังใช้รหัสผ่าน seed เดิม โดยคงบัญชีที่เปลี่ยนรหัสผ่านแล้ว
- เปลี่ยน QR แบบเลขเรียงที่ยังไม่เช็คอินเป็นค่าสุ่ม QR ที่เช็คอินแล้วเก็บไว้เป็นประวัติ
- เพิ่มลิงก์บัตรแบบ token สุ่ม ลิงก์เก่า `ticket.html?id=...` เปิดบัตรไม่ได้อีก
- คงวันงานเดิมเพื่อดูประวัติ เพิ่มวันงานถัดไปจากหน้าจัดการงาน ไม่สร้างวันงานสมมติให้เอง
- ตั้งสถานะชำระเงินของข้อมูลเก่าเป็น `unverified` ไม่ถือว่าราคาบัตรเท่ากับเงินที่รับจริง

หลัง deploy ต้องส่งลิงก์ใหม่ให้ลูกค้าที่ถือบัตรเดิมและยังไม่ได้ใช้ รวมถึง VIP ที่ใช้ไปบางที่นั่ง บัตรเดิมที่พิมพ์ไว้ต้องเปลี่ยน QR ของที่นั่งที่ยังไม่ใช้

## สำรองข้อมูล

ใช้ `pg_dump`/`pg_restore` รุ่นที่รองรับ PostgreSQL ของปลายทาง ตั้งค่า `PGHOST`, `PGPORT`, `PGDATABASE`, `PGUSER`, `PGPASSFILE` และ SSL ตามการเชื่อมต่อ Supabase ของคุณ ห้าม commit ไฟล์รหัสผ่านหรือ dump

```powershell
powershell -File scripts/backup.ps1
```

สคริปต์เก็บ schema public พร้อมข้อมูล ฟังก์ชัน สิทธิ์ และ RLS ใน `backups/` พร้อม SHA-256 ไม่ครอบคลุม storage objects, Supabase Auth, environment variables หรือ LINE configuration ซึ่งต้องสำรองแยกตามบริการนั้น

สำรองก่อนอัปเดตทุกครั้ง และอย่างน้อยทุกวันในช่วงเปิดขาย เก็บสำเนานอกเครื่องที่ควบคุมสิทธิ์เข้าถึง ตรวจ archive อย่างเดียวไม่พอ ต้องทดลอง restore ด้วย

## ทดสอบกู้คืนในฐานข้อมูลใหม่

1. สร้าง Supabase project สำหรับ staging แยกจาก production และยืนยัน host/database ที่จะกู้คืน ห้ามใช้ `--clean` กับฐานข้อมูล production
2. ให้มี schema `extensions` และ extension `pgcrypto` ติดตั้งใน schema นี้ รวมถึง roles `anon`, `authenticated`, `service_role` ตามปกติของ Supabase
3. ตั้งค่า PG environment variables ให้ชี้ staging แล้วรัน:

```powershell
pg_restore --no-owner --exit-on-error --dbname=$env:PGDATABASE backups/concert-YYYYMMDD-HHMMSS.dump
```

4. เปรียบเทียบจำนวน `tickets`, `ticket_codes`, `checkins`, `ticket_audit_logs` และตรวจ RLS/สิทธิ์ฟังก์ชัน
5. ล็อกอินด้วยบัญชีทดสอบใน staging ออกบัตร สแกนซ้ำ และตรวจยอดรายงาน ห้ามใช้ LINE token จริงใน staging เพื่อไม่ส่งข้อความหาลูกค้า
6. กรณีกู้จาก dump รุ่นเก่า รัน security upgrade ใน staging ก่อนนำกลับไปใช้จริง

หาก migration ล้มเหลว คำสั่งอยู่ใน transaction และควร rollback ทั้งชุด ตรวจข้อผิดพลาดก่อน deploy หน้าเว็บใหม่ หากต้องกู้คืนหลังมีรายการใหม่เกิดขึ้น ต้องกระทบยอดรายการเหล่านั้นก่อนสลับฐานข้อมูล ไม่ย้อน dump ทับข้อมูลใหม่ทันที

## ตรวจรับหลัง deploy

- บัญชี admin/issuer/checkin ได้สิทธิ์ตามที่กำหนด ผู้ไม่ล็อกอินอ่านตารางไม่ได้
- สแกน QR เดียวพร้อมกันสองเครื่อง สำเร็จเพียงครั้งเดียว
- กดยกเลิกพร้อมเช็คอิน บัตรต้องไม่ทั้งยกเลิกและรับเข้า
- เปิดบัตรผิดวัน บัตรยกเลิก และบัตรที่ใช้แล้ว ต้องไม่รับเข้า
- ออกหลายใบแล้วตัดเน็ต จากนั้นกู้คืนรายการเดิม ต้องไม่เพิ่มบัตรซ้ำ
- ตรวจหน้า Dashboard เมื่อมีเช็คอินเกิน 200 รายการ และ Export เมื่อบัตรเกิน 500 ใบ
- ทดสอบกล้องบนมือถือจริง โดยใช้ HTTPS และสิทธิ์กล้อง หากเบราว์เซอร์ไม่รองรับ BarcodeDetector ใช้กรอกรหัสสำรอง
- LINE ใช้บัญชี/ผู้รับทดสอบที่คุณควบคุม ตรวจ webhook signature และตรวจคำขอซ้ำ

ชุด `npm test` ใช้ PostgreSQL ผ่าน PGlite และ mock API/DOM; `npm run test:concurrency` ใช้ PostgreSQL ชั่วคราวหลาย connection ในเครื่อง ไม่เชื่อมต่อระบบจริง ทั้งสองชุดไม่แทนการตรวจการตั้งค่า Supabase, Vercel และกล้องบนมือถือจริง
